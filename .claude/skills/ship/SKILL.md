---
name: ship
description: Build, test, and containerize the application
---
# Ship Skill
1. Run `npm run build` to verify the production bundle.
2. Execute `npm test` to ensure no regressions.
3. Run `docker compose build` to prepare the production image.
